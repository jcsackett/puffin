<html>
<head><title>My Blog</title></head>
	<body>
		<h1>My Blog!</h1>
		{% for post in posts %}
		<p>
			<h2><a href="{{ post.url }}">{{ post.title }}</a></h2>
			{{ post.content }}
		</p>
		{% endfor %}
	</body>
</html>